#include <stdio.h>>
#include <string.h>

int main(int argc, char** argv){
    printf("Program execute: %s number of inputs elements: %d", argv[0]), argc()
    printf("Hola %s %s %s %ss, bienvenido!\n", argv[1],argv[2],argv[3]);
    return 0;
}